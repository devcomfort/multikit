---
agent: multikit.generation
---

Based on the user's request, generate a new agent and prompt pair for multikit.
Ensure you follow the naming conventions (`<kit_name>.[<language>.]<agent_name>`) and include all required sections (Philosophy, Goal, Constraints, Execution Steps) in the agent definition.
